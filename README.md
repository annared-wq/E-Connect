
  # E-Connect

  This is a code bundle for E-Connect. The original project is available at https://www.figma.com/design/x5TgtXJT6uQ7esRRpXgLEt/E-Connect.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  